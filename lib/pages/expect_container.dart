import 'package:flutter/material.dart';

class ExpectContainer extends StatelessWidget {
  const ExpectContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Expect new features soon ..."),
      ),
    );
  }
}
